Steps to Run project

Frontend:
1. Go to frontend folder then do npm install
2. npm run dev (to start locally)

Backend:
1. Go to server folder then do npm install
2. Set up MONGO_URL by making .env file (see example file there)
3. npm run dev (to start locally)